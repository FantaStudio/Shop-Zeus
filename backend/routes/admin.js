const { Router } = require(`express`);
const router = Router();

router.get(`/api/v1/admin/products`, async (req, res) => {
  try {
  } catch (err) {
    console.log(err);
  }
});

router.get(`/api/v1/admin/product`, async (req, res) => {
  try {
  } catch (err) {
    console.log(err);
  }
});

module.exports = router;
